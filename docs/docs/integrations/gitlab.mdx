---
title: "GitLab"
description: "Configure a GitLab OAuth application in Towbar's YAML configuration and authorize one account."
---

Towbar supports one GitLab OAuth authorization per workspace. OAuth application identity, client secret, webhook secret, base URL, and private-network policy live in `/etc/towbar/towbar.yml`. PostgreSQL stores the encrypted OAuth grant and account metadata needed to refresh it.

## Configure OAuth

Create an OAuth application in GitLab with the `api` and `read_repository` scopes. Set its callback to the exact API callback URL, then configure:

```yaml title="/etc/towbar/towbar.yml"
integrations:
  gitlab:
    enabled: true
    baseUrl: https://gitlab.com
    allowPrivateNetwork: false
    oauthClientId: <application-id>
    oauthClientSecret: <application-secret>
    oauthRedirectUri: https://towbar.example/v1/core/gitlab/oauth/callback
    webhookSecret: <shared-webhook-secret>
```

Private-network access must be enabled deliberately for an internal self-managed GitLab instance. Validate and restart Towbar after changing these values.

## Authorize and connect repositories

Open **Manage → Integrations → GitLab** and choose **Connect GitLab**. Towbar uses Authorization Code with PKCE, verifies the callback state for the signed-in Admin, and stores only encrypted access and refresh tokens. Reauthorizing replaces the single grant. Disconnecting deletes the encrypted grant and stops new GitLab repository work.

When adding a Repository, select GitLab, then the project and branch. Towbar reads immutable commit archives, processes push and merge-request webhooks idempotently, and reports deployment results without making provider feedback part of deployment success.

## Verify a project connection

Open **Repositories → Add repository**, select GitLab, and confirm that the intended project appears. Map an environment to a branch containing `towbar.yml` and at least one app or resource manifest, then inspect the first sync. The project connection proves repository access; a successful sync proves that Towbar can read and validate that commit. Deployment remains a separate action.

Push a harmless manifest change to a mapped branch and check the resulting sync. If nothing arrives, inspect the GitLab project's webhook deliveries and the public Towbar origin. If authorization expires or project access changes, reconnect the account from **Manage → Integrations → GitLab** and verify the project again. Keep `allowPrivateNetwork` disabled unless the configured self-managed GitLab host intentionally needs private-network access.

<div className="towbar-doc-screenshot">
  <div className="towbar-product-light">
    <img
      src="/assets/release-v2/gitlab-setup-light.jpg"
      alt="GitLab integration settings in Towbar."
      width="2560"
      height="1440"
      loading="lazy"
    />
  </div>
  <div className="towbar-product-dark">
    <img
      src="/assets/release-v2/gitlab-setup-dark.jpg"
      alt="GitLab integration settings in Towbar."
      width="2560"
      height="1440"
      loading="lazy"
    />
  </div>
  <p>
    The GitLab page shows the connected account and lets an Admin reauthorize or
    disconnect it.
  </p>
</div>
