---
title: "AWS"
description: "Enable one runtime AWS identity for resource backups and restores."
---

AWS is optional. Enable it only when a resource backup policy uses AWS S3.

## Prepare AWS access

Create a bucket and an IAM identity for Towbar's backup work. Scope object listing, upload, read, and deletion to the buckets and prefixes in your resource policies; include object-version and KMS permissions when those features are used. Record the bucket's region. Towbar uses a static access-key pair in its protected runtime configuration; temporary AWS sessions are not supported.

```yaml title="/etc/towbar/towbar.yml"
integrations:
  aws:
    enabled: true
    region: us-east-1
    accessKeyId: <access-key-id>
    secretAccessKey: <secret-access-key>
```

Run `sudo towbar config validate` and `sudo towbar restart`.

Towbar shows AWS only when all enabled values pass startup validation and never returns the credential in the UI or API.

<div className="towbar-doc-screenshot">
  <div className="towbar-product-light">
    <img
      src="/assets/release-v2/aws-setup-light.jpg"
      alt="The AWS integration confirms that its runtime configuration is available without exposing credentials."
      width="2560"
      height="1440"
      loading="lazy"
    />
  </div>
  <div className="towbar-product-dark">
    <img
      src="/assets/release-v2/aws-setup-dark.jpg"
      alt="The AWS integration confirms that its runtime configuration is available without exposing credentials."
      width="2560"
      height="1440"
      loading="lazy"
    />
  </div>
  <p>
    Runtime-managed integrations expose availability and documentation without
    returning their credentials.
  </p>
</div>

## Verify backup and restore

Set a managed resource's [backup policy](/docs/backups) to use `aws`, create a backup, and inspect the saved object's verification state. Perform a [fresh-target restore](/docs/restores) before treating the destination as recovery-ready. A **Configured** chip means YAML validation succeeded; it does not prove S3 or KMS access.

If a run fails, check its final stage, region, bucket and prefix, key permissions, object versioning, and KMS policy. Rotate the key in `towbar.yml`, restart Towbar, verify a backup and restore path, and then retire the old key.
