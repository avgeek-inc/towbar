# Platform expansion progress

## Current status

Feature implementation and final local verification are complete for the planned F01–F15 surfaces. Full scope: `docs/plans/platform-expansion-requirements.md`. Execution plan: `docs/plans/platform-expansion.md`. Generated contracts, focused regressions, required integration groups and the production build/start/recovery/image-security gate pass on the final tree. An implemented surface is not a live-provider support claim until its external acceptance evidence is recorded below.

The clean-break schema is consolidated into `001_team_access_v2.sql` with one journal entry. Shared named integrations, repository/environment scopes, discriminated manifests, admission snapshots, execution-time authorization, UI operation surfaces, REST handlers, MCP tools, fixture support, and practical documentation are present. Live cloud accounts, disposable IdPs, and cross-architecture hosts remain external verification boundaries. Server patching and reboot were explicitly deferred from v2.0.0 and removed from the product surface and runtime.

## Decisions

- 2026-09-17: Remote tags and releases confirm latest published v1.7.0, with no v2 release. Preserve the clean-break v2 initial-schema policy; no user database reset.
- 2026-09-17: Existing CI silently omits several security/recovery/Temporal suites. Implemented an owned, disposable, fail-on-skip required test runner, six CI matrix jobs, a production build/boot/restore gate, evidence artifacts and a final Required CI status.
- 2026-09-17: Use an explicit discriminated deployment type, named integration references with scoped grants, immutable admission snapshots and execution-time authority checks. No UI-only workload settings.
- 2026-09-18: Implemented F01–F15 before the broad verification pass. Generated artifacts and decisive lifecycle tests must be refreshed from the final tree before release.
- 2026-09-20: Final local verification passed. Replaced the vulnerable Debian worker runtime with a pinned Wolfi glibc/Node 24 runtime, retained the strict unfixed HIGH/CRITICAL policy, and passed the complete production lifecycle and all three image scans.

## Milestones

| Milestone | Implementation state                                                                                         | Verification state                                                                                                           |
| --------- | ------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------- |
| A         | Complete: shared contracts, named integrations, clean `001`, schemas, fixtures, UI/REST/MCP control surfaces | Contracts, migrations, generated REST/MCP surfaces and negative-policy tests passed                                          |
| B         | Complete: GitLab, six deployment modes, immutable images, separate build servers                             | Local provider/build examples and admission/execution tests passed; live providers and both real architectures pending       |
| C         | Complete: health-gated rolling replacement and restricted Compose runtime                                    | Docker lifecycle, failure, rollback, cancellation and recovery tests passed; sustained live traffic exercise pending         |
| D         | Complete: named storage, app/Compose volumes, eight managed engines                                          | Integrated volume/resource recovery and all-engine isolated restore fixtures passed; live object stores pending              |
| E         | Complete: three external secret providers; enterprise identity removed from v2.0.0 scope                     | Secret-provider lifecycle and leakage tests passed; live provider accounts remain pending                                    |
| F         | Complete: OTLP three-signal collector and Tunnel                                                             | Collector resilience and local lifecycle tests passed; real backends/accounts remain pending                                 |
| G         | Complete locally                                                                                             | Full regression, contracts, production lifecycle, image security and comparison completed; remote CI/live acceptance pending |

## UI / REST / MCP parity ledger

“Implemented” means the final local control path exists. It does not replace the proof named in the final column.

| Capability              | Manifest / configuration  | Service and runtime | UI                              | REST        | MCP                                | Decisive lifecycle proof                                     |
| ----------------------- | ------------------------- | ------------------- | ------------------------------- | ----------- | ---------------------------------- | ------------------------------------------------------------ |
| F01 GitLab              | Implemented               | Implemented         | Implemented                     | Implemented | Implemented                        | Pending cloud/self-managed coexistence and webhook lifecycle |
| F02 Build modes         | Implemented               | Implemented         | Manifest preview and operations | Implemented | Shared workload tools              | Pending real deployment for all six examples                 |
| F03 Compose             | Implemented               | Implemented         | Implemented                     | Implemented | Shared workload tools              | Pending live multi-service partial-failure recovery          |
| F04 Rolling replicas    | Implemented               | Implemented         | Status/history                  | Implemented | Shared deployment tools            | Pending live traffic continuity and restart reconciliation   |
| F05 Build servers       | Implemented               | Implemented         | Manifest preview/status         | Implemented | Shared workload tools              | Pending direct/registry transfer on both architectures       |
| F06 Volume recovery     | Implemented               | Implemented         | Implemented                     | Implemented | Implemented                        | Pending source-volume-loss recovery                          |
| F07 Managed engines     | Implemented               | Implemented         | Implemented                     | Implemented | Implemented                        | Pending representative backup/restore for all eight          |
| F08 R2/custom S3        | Implemented               | Implemented         | Implemented                     | Implemented | Generic integration tools          | Pending permission, CA, multipart, retention matrix          |
| F09 Enterprise identity | Removed from v2.0.0 scope | Removed             | Removed                         | Removed     | Removed                            | Deferred to a later version                                  |
| F10 External secrets    | Implemented               | Implemented         | Implemented                     | Implemented | Generic integration tools          | Pending Infisical and Doppler live version/leakage matrix    |
| F11 Fluent Bit          | Removed from v2.0.0 scope | Removed             | Removed                         | Removed     | Removed                            | No v2.0.0 acceptance claim                                   |
| F12 MCP parity          | Cross-cutting             | Service-layer reuse | N/A                             | Implemented | Implemented                        | Pending full role/key/tool policy matrix                     |
| F13 OTLP all signals    | Implemented               | Implemented         | Implemented                     | Implemented | Generic integration tools          | Pending real backend three-signal isolation/load             |
| F14 Tunnel              | Implemented               | Implemented         | Implemented                     | Implemented | Generic integration/workload tools | Pending disposable Cloudflare lifecycle                      |
| F15 Patching/reboot     | Deferred for v2.0.0       | Removed             | Removed                         | Removed     | Removed                            | No v2.0.0 acceptance claim                                   |

## Verification log

- `git ls-remote --tags origin 'v2*' 'v1.7*'`: v1.7.0 found; no v2 tag.
- `gh release list --repo avgeek-inc/towbar --limit 4`: latest published v1.7.0 on 2026-09-10.
- Inspected current manifests, auth policy, deployment/backup architecture, package lock inputs and CI. Historical final-verification report is not reused as a new passing gate.

## Open defects and prerequisites

- UUID advisory resolved with a narrow `gaxios@6.7.1>uuid` override to 11.1.1. Production audit reports zero vulnerabilities; all storage SDK emulator tests passed. This does not substitute for container-image scanning or live-cloud IAM acceptance.
- Required CI workflow is implemented and validated locally. Remote CI execution and requiring the aggregate check in branch protection remain unverified; no push or ruleset mutation was performed.
- Production Temporal replacement is locally verified: digest-pinned server/admin-tools 1.31.1, additive schema initialization, explicit namespace setup, a mounted dynamic configuration and HTTP readiness. Fresh startup and persisted workflow recovery passed. The API stays private to the Compose network; the operator UI stays on loopback even when the app bind address changes. No existing user database was altered.
- The earlier Debian worker-image findings are preserved below as historical evidence. The final tree uses a pinned Wolfi glibc runtime and the complete production gate now passes with zero HIGH/CRITICAL findings in API, worker and web images.
- External prerequisites and live acceptance boundaries are listed in the plan. None is a reason to stop useful local implementation.

## Next work

Keep mock/protocol, Docker, disposable-provider and live-external evidence separate. Before a public release, run the remote CI matrix on linux/amd64, require its aggregate check in branch protection, and complete the live-provider and disposable-host acceptance boundaries that cannot be proven from a local fixture.

## Executed gates on 17 September 2026

Evidence directories below are beneath `tmp/verification/` (ignored by Git); each includes command logs and `results.json`. The future CI jobs upload the same records.

| Command                                                                     | Outcome                                                                                                                                               | Evidence directory                                                 |
| --------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------ |
| `pnpm test:verification-runner`                                             | 8 passed; proves rejection of empty/skipped/failed/timed-out suites and inherited credential removal                                                  | Runner tests remove their own temporary evidence after assertions  |
| `node tools/verification/run.mjs api`                                       | 300 API tests, 2 database tests, 4 workflow tests; zero skips                                                                                         | `api-0af78438-1640-4b65-9b1d-08596345791a`                         |
| `node tools/verification/run.mjs docker`                                    | 141 deployer tests, zero skips; 8 Python protocol tests                                                                                               | `docker-00b7d2d5-2036-4f54-a37f-f79d92ff1088`                      |
| `node tools/verification/run.mjs app`                                       | API/Temporal/SSH lifecycle, persistent files, PR previews, HTTPS, cleanup passed                                                                      | `app-1e8a5a11-b1c2-4607-85ea-3007a3d44a65`                         |
| `node tools/verification/run.mjs resources`                                 | Integrated resource lifecycle and direct Redis backup/restore passed                                                                                  | `resources-3bde3aa4-5d28-475c-8d38-e5fe4b62a115`                   |
| `node tools/verification/run.mjs forwarding`                                | Installation, delivery and resilience passed; 80,001-line bounded run, one auth attempt, unrelated application work passed                            | `forwarding-5f844a7b-4afd-411b-89a5-becc9303d6da`                  |
| `node tools/verification/run.mjs scanner`                                   | Production Trivy scan through Linux SSH; permission negative control and temporary archive cleanup passed                                             | `scanner-bd3a9d80-f07e-404d-bd9e-c99ff74dd510`                     |
| `node tools/verification/production.mjs`                                    | Production images built; packaged non-root PostgreSQL client backup/restore passed; fresh Compose healthy; 10 onboarding/authentication checks passed | `production-ebdb09fb-1580-4309-b171-4d3001df3977`                  |
| `pnpm verify`, `pnpm docs:api:check`, `actionlint .github/workflows/ci.yml` | Passed after adding the new test environment variables to Turbo's declared inputs                                                                     | `quality-c30e02a6-249e-49e2-bc54-39cd8a226f13`                     |
| `pnpm audit --prod --json`                                                  | Zero info/low/moderate/high/critical findings across 865 dependencies                                                                                 | `/tmp/towbar-expansion-audit.json`; also enforced in `pnpm verify` |

All mandatory TAP suites above ran without skipped tests. The ordinary fast `pnpm test` inside the quality gate retains optional-infrastructure skips; those paths are exercised in the dedicated required jobs. Local execution was macOS arm64 with Linux Docker targets. Linux amd64 CI, real cloud accounts and physical passkeys are not claimed by this pass.

All runner-owned test services, production stack volumes and production image tags were removed. The pre-existing `towbar-v2-tests` database and BuildKit container remained running. Local UI fixture processes were not changed.

## Resolved during this pass

- Required TAP summaries now fail on nonzero skip, failure, cancellation or TODO counts, missing summaries and empty runs.
- Production verification uses unique Compose/image/network/credential context and ignores workspace `.env`; it does not overwrite local configuration.
- Packaged backup/restore verification accepts the unique production image tag instead of depending on an already-built shared tag.
- Timeout handling terminates the process group, and SSH test targets carry the runner ownership label for cleanup after an interrupted harness.
- Fixed CI shell lint's JavaScript interpolation false positive and declared test-only environment variables in Turbo.
- Formatted the updated lockfile; final formatting and API contracts passed.

## Production Temporal follow-up evidence

The upstream [Docker image repository](https://github.com/temporalio/docker-builds) marks auto-setup deprecated. The [server 1.31.1 release](https://github.com/temporalio/temporal/releases/tag/v1.31.1) contains dependency/toolchain security updates. Exact available multiarch indexes:

- Server: `temporalio/server:1.31.1@sha256:5728db90c9883f5b2a7b8ea0bcdfb84edbc9f6c14479cd3354957aa77d4b8bac`.
- Admin tools: `temporalio/admin-tools:1.31.1@sha256:0fa94393f6254dea05df1cdacb7a8ee95f6fb51a2ffbde0c6847a73037f788c7`.

Both indexes publish linux/amd64 and linux/arm64. Inspected upstream `config/docker.yaml`, `docker/scripts/sh/entrypoint.sh`, `docker/targets/server.Dockerfile`, `docker/targets/admin-tools.Dockerfile` and `tools/common/schema/setuptask.go` at v1.31.1. The new server image only carries `temporal-server`; health checks and namespace setup cannot assume the CLI is present. Schema setup is idempotent and does not downgrade a newer schema version. Use official schema tooling, never a reset. The server's `--allow-no-auth` behavior must be considered explicitly with internal-only networking; do not silently expose its ports.

Completed replacement evidence: `tmp/verification/production-2c7f36de-b4e6-444e-a4e9-7e43037bfbb0/results.json`. Actual production images, packaged backup/restore, fresh Compose startup and all 10 onboarding checks passed. The recovery check then recreated Temporal and restarted PostgreSQL, reran the initialization jobs, resumed the same workflow run without repeating its completed activity, and replayed the full history. The initial failure was a missing upstream-default dynamic configuration file, not a readiness endpoint mismatch; the explicit read-only configuration mount resolved it. Failed attempts remain in their separate evidence directories.

Final follow-up gates: `tmp/verification/quality-1539553b-7fa5-423d-8fb8-558fd4b3af7f/results.json`: `pnpm verify`, API/MCP contract checks, Actionlint and infrastructure ShellCheck passed. No application code changed in this follow-up, so the six previously completed integration groups remain applicable; the affected production lifecycle was rerun. All owned containers, volumes and image tags were removed after the checks.

## Runtime image hardening follow-up

- Pinned the official Node Alpine and Trixie multiarch indexes and Go 1.26.8 compiler index. Worker runtime moved from Debian Bookworm to Trixie; the Go builder stays on Bookworm but only its static binaries enter the final runtime. Removed bundled npm, Corepack and Yarn from final images only; build stages retain required tooling. Runtime package upgrades apply supported distribution security updates.
- Production verification now scans API/worker/web images with pinned Trivy and fails on HIGH or CRITICAL advisories, including unfixed advisories. Scanner errors fail too. Every image is checked even if another fails, and timed-out scanner cleanup is scoped to both its unique name and run label. Raw JSON and logs remain evidence. No vulnerability ignore list was added.
- Evidence `runtime-hardening-a6237fa8-f512-475c-861f-17284c02181d`: builds and non-root/runtime-tool checks passed. API/web each have 0 HIGH/CRITICAL; worker has 53 HIGH + 1 CRITICAL package/advisory matches, representing 15 distinct CVEs, all without a listed fixed version in that distribution. This is not a security-clear claim.
- The same run exercised production packaging, actual PostgreSQL backup/restore, fresh stack onboarding and Temporal recreation/replay after PostgreSQL restart. All functional checks passed in `production-a641b7b7-9929-493c-a39d-bf9febdf4a55` (that process started before the newly added image gate was loaded). A separate final invocation verifies the new gate itself; do not relabel the earlier process as exercising it.
- `scout-patched-runtime-83c4c821-f7a0-4791-b595-08a432b15bd1`: actual Linux arm64 Go 1.26.8 vet and uncached race tests passed; Actionlint passed. Worker build produced both Linux amd64/arm64 Scout binaries. Architecture execution remains distinct from cross-compilation.
- Remaining worker findings include OpenSSH client re-exchange (critical scanner score), source-package matches for utilities, curl and Perl. They require vendor/runtime-path review or updated packages. The required production gate must remain red until they are resolved or narrowly justified by an explicit reviewed policy; ordinary successful functional tests do not override it.

Final strengthened production gate: `production-86d746df-5120-4948-90bd-436e4f776714`. Build, packaged backup/restore, onboarding and Temporal recovery all passed. API and web security scans passed; worker security scan exited 1 with the same 53 HIGH / 1 CRITICAL findings. The web scan still ran after the worker failed, proving that findings do not hide later image reports. Overall production verification correctly failed; it must not be reported green. CI artifact configuration excludes the 1.3 GiB scanner cache and retains the actual JSON reports/logs. Ten verifier tests, Actionlint, documentation checks and whitespace checks passed.

## Coolify and Dokploy comparison update on 20 September 2026

This refresh uses current official documentation and compares documented capability categories, not installed-product performance or security. “Not verified” means the reviewed official documentation did not establish equivalent first-party behavior; it does not prove absence. Towbar entries describe the final local tree and retain the external acceptance boundaries in the parity ledger.

| Expansion capability                    | Towbar final local tree                                                                                                                                                             | Coolify official documentation                                                                                                                                              | Dokploy official documentation                                                                                                                                                                                    | Assessment                                                                                                         |
| --------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------ |
| F01 GitLab                              | GitLab.com and validated self-managed connections, discovery, immutable fetch, webhooks, previews and status feedback beside GitHub                                                 | Supports multiple Git providers and Git-based deployments through its [source model](https://coolify.io/docs/applications/sources/overview)                                 | GitHub, GitLab, Bitbucket, Gitea and generic Git; GitLab OAuth and automation are documented in [Providers](https://docs.dokploy.com/docs/core/providers) and [GitLab](https://docs.dokploy.com/docs/core/gitlab) | Core category parity; Towbar's manifest/environment and webhook safety rules are deliberate differences            |
| F02 build/deployment modes              | Dockerfile, static, immutable OCI image, Railpack, Nixpacks and Cloud Native Buildpacks                                                                                             | Dockerfile, static, image, Railpack and Nixpacks are documented in [Build applications](https://coolify.io/docs/applications/builds/overview)                               | Dockerfile, static, Railpack, Nixpacks and Heroku/Paketo buildpacks are documented in [Build Type](https://docs.dokploy.com/docs/core/applications/build-type)                                                    | Towbar covers the requested union; live multi-architecture builder acceptance remains                              |
| F03 Compose                             | Repository-owned Compose plus overrides/profiles, bounded paths, service ingress/logs/status/actions and unsafe-option rejection                                                    | Git or control-plane definitions with service builds/images, health, networking and storage in [Docker Compose](https://coolify.io/docs/applications/builds/docker-compose) | First-class Compose and raw definitions through [Providers](https://docs.dokploy.com/docs/core/providers)                                                                                                         | Core category parity; Towbar intentionally keeps repository manifests authoritative and rejects unsafe host access |
| F04 rolling replacement                 | Health-gated start-first rolling replacement for eligible services, replicas, surge/unavailable controls and automatic rollback; explicit recreate/maintenance for unsafe workloads | Eligible application replacement and health behavior are described in the [build/deployment model](https://coolify.io/docs/core/build-deployment-model); Compose differs    | Replica and rolling behavior centers on Docker Swarm/cluster configuration                                                                                                                                        | Towbar now has a first-party single-server rolling path but does not claim cross-server HA                         |
| F05 build servers                       | Manifest-selected registered build host, capability/architecture checks, explicit fallback policy, direct or registry transfer and digest verification                              | Dedicated build servers use a registry and matched architecture in [Build servers](https://coolify.io/docs/core/infrastructure/servers/build-servers)                       | Dedicated build servers are documented for remote-server workflows                                                                                                                                                | Category parity with stricter manifest admission; physical amd64/arm64 transfer remains external acceptance        |
| F06 application/Compose volume recovery | Removed from v2.0.0; Storage is an inventory of persistent volumes and does not expose backup or restore actions                                                                    | Persistent-storage backups are supported; [Docker concepts](https://coolify.io/docs/core/docker-and-containers) distinguishes persistence from backup                       | Named-volume backup/restore to S3 and optional container stop are documented in [Volume Backups](https://docs.dokploy.com/docs/core/volume-backups)                                                               | Deliberate v2.0.0 scope gap                                                                                        |
| F07 managed databases                   | PostgreSQL, MySQL, MariaDB, MongoDB, Redis, Dragonfly, KeyDB and ClickHouse with engine-aware lifecycle/recovery                                                                    | The requested eight-engine set is represented in [Databases](https://coolify.io/docs/databases/)                                                                            | PostgreSQL, MySQL, MariaDB, MongoDB and Redis in [Databases](https://docs.dokploy.com/docs/core/databases)                                                                                                        | Towbar matches the requested Coolify/Dokploy union; live engine/version recovery matrix remains                    |
| F08 backup storage                      | Named S3, R2, generic S3-compatible, GCS and Azure Blob integrations with scoping and encrypted credentials                                                                         | S3-compatible backup destinations are documented                                                                                                                            | S3 destinations are documented for database and volume backups                                                                                                                                                    | Towbar has broader first-party destination breadth; real IAM, CA and retention acceptance remains                  |
| F09 OIDC/SAML/SCIM                      | Removed from v2.0.0; UI, protocol routes, provider contracts, runtime plugins and schema objects were removed                                                                       | SSO is documented; equivalent generic SCIM behavior was not verified in reviewed docs                                                                                       | OIDC, SAML and SCIM are documented as [Enterprise](https://docs.dokploy.com/docs/core/enterprise) capabilities                                                                                                    | Deliberate v2.0.0 scope gap                                                                                        |
| F10 external secrets                    | Infisical and Doppler references resolved at execution with scoped grants and leakage controls                                                                                      | Equivalent native provider breadth was not verified in reviewed docs                                                                                                        | Vault/OpenBao, Infisical, AWS, Doppler, Azure and Scaleway are documented in [Secrets Providers](https://docs.dokploy.com/docs/core/secrets-providers)                                                            | Towbar supports two external secret providers; Dokploy documents a broader catalogue                               |
| F11 custom Fluent Bit                   | Removed from v2.0.0; provider contracts, manifest fields, runtime, UI, REST/MCP delivery tests and schema objects were removed                                                      | Axiom, New Relic and custom Fluent Bit are documented in [Log drains](https://coolify.io/docs/core/observability/log-drains/overview)                                       | Equivalent first-party custom Fluent Bit management was not verified                                                                                                                                              | Deliberate v2.0.0 scope gap                                                                                        |
| F12 MCP parity                          | 70 generated task-oriented tools using the same services, RBAC, operation IDs, status/history/cancel/recovery paths                                                                 | Official MCP is documented in [MCP](https://coolify.io/docs/mcp/overview)                                                                                                   | First-party MCP was not verified in reviewed docs                                                                                                                                                                 | Towbar and Coolify both expose MCP; tool counts do not imply semantic equivalence                                  |
| F13 OTLP logs/metrics/traces            | One named integration can route all three signals with bounded attributes and workload/environment identity                                                                         | Log drains and host monitoring are documented; equivalent first-party three-signal OTLP management was not verified                                                         | Monitoring is documented; equivalent first-party three-signal OTLP management was not verified                                                                                                                    | Towbar implements the requested three-signal path; real backend isolation/load remains                             |
| F14 Cloudflare Tunnel                   | Named Cloudflare integration plus manifest ingress mode, deterministic configuration, token lifecycle and rollback                                                                  | Cloudflare-focused networking exists, but equivalent per-workload Tunnel lifecycle was not verified in reviewed docs                                                        | Equivalent first-party Tunnel lifecycle was not verified                                                                                                                                                          | Towbar has the requested control path; a disposable account/domain lifecycle remains                               |
| F15 patch/reboot                        | Deferred from v2.0.0; the UI, REST/MCP routes, Temporal workflows and schema were removed                                                                                           | Package patching is listed in [server management](https://coolify.io/docs/core/infrastructure/servers/overview)                                                             | Server update/reboot equivalence was not verified in reviewed docs                                                                                                                                                | Deliberate v2.0.0 scope gap                                                                                        |

The expansion closes the specific GitLab, build-mode, Compose, rolling, build-server, database, storage, external-secret, MCP, OTLP and Tunnel gaps identified in the earlier comparison. It does not add application-volume recovery, custom Fluent Bit, enterprise identity, server patching/reboot, Dokploy's broader generic-Git catalogue, Vault/Azure/Scaleway secret providers, custom role designer or Swarm clustering, and it does not add Coolify's service-template catalogue or cloud-VM provisioning. Those are separate product choices rather than unfinished v2.0.0 work.

## Final-tree verification on 20 September 2026

All in-scope F01–F15 implementation surfaces are present in the final local tree across manifests/configuration, shared services and runtime, UI, REST and MCP where the capability has an operator control path. F09, F11 and F15 are explicitly removed from the v2.0.0 scope. This records code-complete local implementation, not live-provider certification. No external provider account, production host, DNS record, repository connection or branch-protection setting was changed during this pass.

| Gate                                                                         | Outcome                                                                                                                                                                                                                                    | Evidence                                                           |
| ---------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------ |
| `node tools/verification/run.mjs api`                                        | Passed: 300 API, 2 database and 4 Temporal tests; zero skips                                                                                                                                                                               | `tmp/verification/api-224eac8b-7a92-49a7-9b3f-8586ad53059f`        |
| `node tools/verification/run.mjs docker`                                     | Passed: 222 required deployer tests plus the Python log gateway protocol suite; zero skips                                                                                                                                                 | `tmp/verification/docker-ac1bf1ec-d549-4c12-bf6e-80cfcb6f1685`     |
| `node tools/verification/run.mjs app`                                        | Passed: API/Temporal/SSH lifecycle, persistent-file, preview and cleanup checks                                                                                                                                                            | `tmp/verification/app-4ea1561c-9fae-424b-b91f-2bf2548ebc42`        |
| `node tools/verification/run.mjs resources`                                  | Passed: integrated resource lifecycle and direct Redis/KeyDB recovery paths                                                                                                                                                                | `tmp/verification/resources-d9df6a18-f387-46ad-b865-281a15899a82`  |
| `node tools/verification/run.mjs forwarding`                                 | Passed: installation, delivery and bounded resilience; 80,001 lines, 667 accepted batches, 71.19 MiB Vector peak, 12.61 MiB controller peak, one authentication attempt, three transient attempts and unrelated application work completed | `tmp/verification/forwarding-12d0fa17-e6b9-4228-828b-11774e2e0032` |
| `node tools/verification/run.mjs scanner`                                    | Passed: non-root private-archive scan, Linux permission negative control and temporary-archive cleanup                                                                                                                                     | `tmp/verification/scanner-acd77a1c-725f-4f58-be06-d6933d5cfcc7`    |
| `pnpm verify`                                                                | Passed: generated docs sync, formatting, lint, typechecks, verifier tests, all workspace tests, production dependency audit and all production builds                                                                                      | Console gate on the final tree                                     |
| `pnpm docs:api:check`                                                        | Passed: 231 response handlers, 161 OpenAPI operations and 70 MCP tools                                                                                                                                                                     | Generated-contract check on the final tree                         |
| `actionlint .github/workflows/*.yml`                                         | Passed                                                                                                                                                                                                                                     | Final-tree workflow lint                                           |
| `shellcheck infra/deploy-release.sh infra/postgres/*.sh infra/temporal/*.sh` | Passed                                                                                                                                                                                                                                     | Final-tree infrastructure shell lint                               |
| `docker compose --env-file .env.example config --quiet`                      | Passed without writing a local secret file                                                                                                                                                                                                 | Final-tree Compose structure check                                 |
| `pnpm audit --prod --audit-level=moderate`                                   | Passed with no known vulnerabilities after moving Secrets Manager to the current workspace-catalog AWS SDK version                                                                                                                         | Final production dependency graph                                  |
| `node tools/verification/production.mjs`                                     | Passed: image build, packaged backup/restore, startup, onboarding, Compose state and Temporal recovery. API, worker and web image scans each report zero HIGH/CRITICAL findings.                                                           | `tmp/verification/production-08ee676e-2898-4b0a-b811-211683d9b00f` |

The earlier Debian worker image failed on 53 HIGH and 1 CRITICAL package/advisory matches representing 15 CVEs, including `CVE-2026-60002` in OpenSSH. Towbar did not suppress or waive them. The final worker runtime is a pinned Wolfi multiarch image with exact direct package versions, glibc for Temporal's native bridge, Node 24 minimal, OpenSSH, curl and CA certificates. A runtime smoke check proved non-root execution, Temporal bridge loading, SSH/SCP/key tooling and archive creation before the complete production verifier passed. The HIGH/CRITICAL gate and its unfixed-advisory policy remain unchanged.
