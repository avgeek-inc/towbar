$kind: http-request
id: 7bfd995e-2437-5ea6-aaad-163675417c45
description:
  "Required permissions: repository.read. Personal keys are capped by the current member
  role; team keys use explicit grants."
url: "{{baseUrl}}/sources?q=&view=all&sync=never&autoDeploy=enabled"
method: GET
headers:
  Accept: application/json
queryParams:
  - key: q
    value: ""
  - key: view
    value: all
  - key: sync
    value: never
  - key: autoDeploy
    value: enabled
order: 1000
