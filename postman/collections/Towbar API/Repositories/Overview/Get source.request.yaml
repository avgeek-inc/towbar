$kind: http-request
id: c66406c1-c715-5adb-9cdf-2e4bc5404f27
description:
  "Required permissions: repository.read. Personal keys are capped by the current member
  role; team keys use explicit grants."
url: "{{baseUrl}}/sources/:sourceId"
method: GET
headers:
  Accept: application/json
pathVariables:
  - key: sourceId
    value: "{{sourceId}}"
    description: Required.
order: 2000
