$kind: http-request
id: ce89bc3c-8bb8-5b1a-90a2-d9c1cc01b3a9
description:
  "Required permissions: secret.update. Personal keys are capped by the current member
  role; team keys use explicit grants."
url: "{{baseUrl}}/sources/:ownerId/secrets/:environment/:stage"
method: PATCH
headers:
  Accept: application/json
  Content-Type: application/json
pathVariables:
  - key: ownerId
    value: "{{ownerId}}"
    description: Required.
  - key: environment
    value: "{{environment}}"
    description: Required.
  - key: stage
    value: "{{stage}}"
    description: Required.
body:
  type: json
  content: |-
    {
      "expectedRevision": "00000000-0000-4000-8000-000000000000",
      "set": {},
      "delete": []
    }
order: 2000
