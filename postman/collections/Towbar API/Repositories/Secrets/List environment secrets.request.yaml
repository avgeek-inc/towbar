$kind: http-request
id: aacebf6e-971d-5aa5-89f3-dbf80095fde2
description:
  "Required permissions: secret.list. Personal keys are capped by the current member
  role; team keys use explicit grants."
url: "{{baseUrl}}/sources/:ownerId/secrets?environment="
method: GET
headers:
  Accept: application/json
queryParams:
  - key: environment
    value: ""
pathVariables:
  - key: ownerId
    value: "{{ownerId}}"
    description: Required.
order: 1000
