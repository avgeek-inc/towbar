$kind: http-request
id: 78c54fe9-2556-5876-9e93-9a4163fd5f4b
description:
  "Required permissions: repository.read. Personal keys are capped by the current member
  role; team keys use explicit grants."
url: "{{baseUrl}}/sources/:sourceId/environments/:environmentId/manifest"
method: GET
headers:
  Accept: application/json
pathVariables:
  - key: sourceId
    value: "{{sourceId}}"
    description: Required.
  - key: environmentId
    value: "{{environmentId}}"
    description: Required.
order: 7000
