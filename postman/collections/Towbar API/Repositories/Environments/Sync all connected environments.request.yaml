$kind: http-request
id: 5dff4e9c-3548-54bf-9485-dc67c0decf63
description:
  "Required permissions: repository.sync. Personal keys are capped by the current member
  role; team keys use explicit grants."
url: "{{baseUrl}}/sources/:sourceId/environments/syncs"
method: POST
headers:
  Accept: application/json
pathVariables:
  - key: sourceId
    value: "{{sourceId}}"
    description: Required.
order: 6000
