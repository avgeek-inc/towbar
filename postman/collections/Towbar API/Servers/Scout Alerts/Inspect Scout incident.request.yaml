$kind: http-request
id: 0e02dd68-50d3-5f18-9777-decb1b01e3a4
description:
  "Required permissions: alert.read. Personal keys are capped by the current member role;
  team keys use explicit grants."
url: "{{baseUrl}}/servers/:serverId/scout-alerts/incidents/:incidentId"
method: GET
headers:
  Accept: application/json
pathVariables:
  - key: serverId
    value: "{{serverId}}"
    description: Required.
  - key: incidentId
    value: "{{incidentId}}"
    description: Required.
order: 6000
