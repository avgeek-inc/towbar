$kind: http-request
id: 565d4c4d-2f4d-5b04-98b0-b090dc38d09f
description:
  "Required permissions: server.credentials. Personal keys are capped by the current
  member role; team keys use explicit grants."
url: "{{baseUrl}}/servers/:serverId/host-keys"
method: GET
headers:
  Accept: application/json
pathVariables:
  - key: serverId
    value: "{{serverId}}"
    description: Required.
order: 5000
