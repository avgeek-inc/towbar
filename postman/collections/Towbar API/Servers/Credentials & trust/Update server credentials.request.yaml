$kind: http-request
id: 63cfbb55-fb5c-5ac9-bbdf-090edfcf6b15
description:
  "Required permissions: server.credentials. Personal keys are capped by the current
  member role; team keys use explicit grants."
url: "{{baseUrl}}/servers/:serverId/credentials"
method: PATCH
headers:
  Accept: application/json
  Content-Type: application/json
pathVariables:
  - key: serverId
    value: "{{serverId}}"
    description: Required.
body:
  type: json
  content: |-
    {
      "expectedRevision": "00000000-0000-4000-8000-000000000000",
      "set": {},
      "delete": []
    }
order: 2000
