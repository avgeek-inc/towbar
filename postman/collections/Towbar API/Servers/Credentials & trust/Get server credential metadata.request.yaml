$kind: http-request
id: f45e04ed-7ceb-518c-ae63-9980088e198a
description:
  "Required permissions: server.credentials. Personal keys are capped by the current
  member role; team keys use explicit grants."
url: "{{baseUrl}}/servers/:serverId/credentials"
method: GET
headers:
  Accept: application/json
pathVariables:
  - key: serverId
    value: "{{serverId}}"
    description: Required.
order: 1000
