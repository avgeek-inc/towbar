$kind: http-request
id: fdffa30d-10b7-58f7-b4eb-a3d38f035935
description:
  "Required permissions: server.read. Personal keys are capped by the current member
  role; team keys use explicit grants."
url: "{{baseUrl}}/servers/:serverId/capacity"
method: GET
headers:
  Accept: application/json
pathVariables:
  - key: serverId
    value: "{{serverId}}"
    description: Required.
order: 1000
