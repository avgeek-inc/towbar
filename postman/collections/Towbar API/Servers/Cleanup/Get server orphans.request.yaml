$kind: http-request
id: e70cf833-3913-546a-aa9e-54c48c4fbfa6
description:
  "Required permissions: server.read. Personal keys are capped by the current member
  role; team keys use explicit grants."
url: "{{baseUrl}}/servers/:serverId/orphans"
method: GET
headers:
  Accept: application/json
pathVariables:
  - key: serverId
    value: "{{serverId}}"
    description: Required.
order: 1000
