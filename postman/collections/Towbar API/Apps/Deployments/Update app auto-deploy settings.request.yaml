$kind: http-request
id: dd843ac4-00e9-5607-a665-f00967c13c9c
description:
  "Required permissions: deployment.create. Personal keys are capped by the current
  member role; team keys use explicit grants."
url: "{{baseUrl}}/apps/:appId/auto-deploy-control"
method: PATCH
headers:
  Accept: application/json
  Content-Type: application/json
pathVariables:
  - key: appId
    value: "{{appId}}"
    description: Required.
body:
  type: json
  content: |-
    {
      "paused": true
    }
order: 2000
