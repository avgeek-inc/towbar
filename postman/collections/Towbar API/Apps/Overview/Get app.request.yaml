$kind: http-request
id: 8aecd008-bdac-5462-a81d-8a422e61286c
description:
  "Required permissions: workload.read. Personal keys are capped by the current member
  role; team keys use explicit grants."
url: "{{baseUrl}}/apps/:appId"
method: GET
headers:
  Accept: application/json
pathVariables:
  - key: appId
    value: "{{appId}}"
    description: Required.
order: 2000
