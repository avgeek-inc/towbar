$kind: http-request
id: 75b083d9-360b-521f-9859-030bad2921f3
description:
  "Required permissions: deployment.create. Personal keys are capped by the current
  member role; team keys use explicit grants."
url: "{{baseUrl}}/apps/:appId/actions/refresh-external-secrets"
method: POST
headers:
  Accept: application/json
  Idempotency-Key: string
pathVariables:
  - key: appId
    value: "{{appId}}"
    description: Required.
order: 3000
