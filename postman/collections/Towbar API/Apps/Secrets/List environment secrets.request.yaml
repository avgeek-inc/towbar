$kind: http-request
id: 0d679621-0005-5d1d-9402-855b0384215b
description:
  "Required permissions: secret.list. Personal keys are capped by the current member
  role; team keys use explicit grants."
url: "{{baseUrl}}/apps/:ownerId/secrets?environment="
method: GET
headers:
  Accept: application/json
queryParams:
  - key: environment
    value: ""
pathVariables:
  - key: ownerId
    value: "{{ownerId}}"
    description: Required.
order: 1000
