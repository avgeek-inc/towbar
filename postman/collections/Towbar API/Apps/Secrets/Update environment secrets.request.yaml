$kind: http-request
id: f134b59f-53dc-56c6-90e7-57785d37efbd
description:
  "Required permissions: secret.update. Personal keys are capped by the current member
  role; team keys use explicit grants."
url: "{{baseUrl}}/apps/:ownerId/secrets/:environment/:stage"
method: PATCH
headers:
  Accept: application/json
  Content-Type: application/json
pathVariables:
  - key: ownerId
    value: "{{ownerId}}"
    description: Required.
  - key: environment
    value: "{{environment}}"
    description: Required.
  - key: stage
    value: "{{stage}}"
    description: Required.
body:
  type: json
  content: |-
    {
      "expectedRevision": "00000000-0000-4000-8000-000000000000",
      "set": {},
      "delete": []
    }
order: 2000
