$kind: http-request
id: e99b997e-b723-5ec8-90af-48e6d3062dd3
description:
  "Required permissions: secret.list. Personal keys are capped by the current member
  role; team keys use explicit grants."
url: "{{baseUrl}}/apps/:ownerId/secrets/readiness"
method: GET
headers:
  Accept: application/json
pathVariables:
  - key: ownerId
    value: "{{ownerId}}"
    description: Required.
order: 2000
