$kind: http-request
id: fe5542b6-cffe-55c9-b1ba-06d025226f07
description:
  "Required permissions: workload.operate. Personal keys are capped by the current member
  role; team keys use explicit grants."
url: "{{baseUrl}}/apps/:appId/actions/run-job"
method: POST
headers:
  Accept: application/json
  Idempotency-Key: string
  Content-Type: application/json
pathVariables:
  - key: appId
    value: "{{appId}}"
    description: Required.
body:
  type: json
  content: |-
    {
      "name": "example"
    }
order: 2000
