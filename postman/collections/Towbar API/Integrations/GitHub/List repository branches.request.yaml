$kind: http-request
id: 10912be4-a6cc-5ba1-b555-82e9cedeba73
description:
  "Required permissions: githubInstallation.read. Personal keys are capped by the current
  member role; team keys use explicit grants."
url: "{{baseUrl}}/github/branches?owner=example-inc&repository=example-app"
method: GET
headers:
  Accept: application/json
queryParams:
  - key: owner
    value: example-inc
  - key: repository
    value: example-app
order: 5000
