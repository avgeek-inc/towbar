$kind: http-request
id: de712444-df65-5916-bf94-7b4bb4a6ab03
description:
  "Required permissions: repository.connect. Personal keys are capped by the current
  member role; team keys use explicit grants."
url: "{{baseUrl}}/gitlab/branches?integration=string&owner=example-inc&repository=example-app"
method: GET
headers:
  Accept: application/json
queryParams:
  - key: integration
    value: string
  - key: owner
    value: example-inc
  - key: repository
    value: example-app
order: 4000
