$kind: http-request
id: bec6db7c-974f-59e9-b3b5-376ef089c48e
description:
  "Required permissions: repository.connect. Personal keys are capped by the current
  member role; team keys use explicit grants."
url: "{{baseUrl}}/gitlab/repositories?integration=string&page=1"
method: GET
headers:
  Accept: application/json
queryParams:
  - key: integration
    value: string
  - key: page
    value: "1"
order: 2000
