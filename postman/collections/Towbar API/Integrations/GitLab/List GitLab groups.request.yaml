$kind: http-request
id: b526ed92-e5db-5937-a671-c940bc82875f
description:
  "Required permissions: repository.connect. Personal keys are capped by the current
  member role; team keys use explicit grants."
url: "{{baseUrl}}/gitlab/groups?integration=string&page=1"
method: GET
headers:
  Accept: application/json
queryParams:
  - key: integration
    value: string
  - key: page
    value: "1"
order: 3000
