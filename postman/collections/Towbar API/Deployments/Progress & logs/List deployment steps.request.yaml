$kind: http-request
id: d8318ae2-8b1d-5812-94ee-237031f39a1e
description:
  "Required permissions: deployment.read. Personal keys are capped by the current member
  role; team keys use explicit grants."
url: "{{baseUrl}}/deployments/:deploymentId/steps"
method: GET
headers:
  Accept: application/json
pathVariables:
  - key: deploymentId
    value: "{{deploymentId}}"
    description: Required.
order: 1000
