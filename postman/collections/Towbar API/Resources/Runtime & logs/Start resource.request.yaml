$kind: http-request
id: e54a4e05-af24-53e2-a20a-c743c3fdae5c
description:
  "Required permissions: workload.operate. Personal keys are capped by the current member
  role; team keys use explicit grants."
url: "{{baseUrl}}/resources/:resourceId/actions/start"
method: POST
headers:
  Accept: application/json
  Idempotency-Key: string
  Content-Type: application/json
pathVariables:
  - key: resourceId
    value: "{{resourceId}}"
    description: Required.
body:
  type: json
  content: |-
    {
      "service": "string"
    }
order: 4000
