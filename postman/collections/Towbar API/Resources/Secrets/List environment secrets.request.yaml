$kind: http-request
id: f8a06f53-fe4f-5859-bae3-82e0f7c23f7e
description:
  "Required permissions: secret.list. Personal keys are capped by the current member
  role; team keys use explicit grants."
url: "{{baseUrl}}/resources/:ownerId/secrets?environment="
method: GET
headers:
  Accept: application/json
queryParams:
  - key: environment
    value: ""
pathVariables:
  - key: ownerId
    value: "{{ownerId}}"
    description: Required.
order: 1000
