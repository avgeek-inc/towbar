$kind: http-request
id: 5d8df54c-ebc1-55a5-b981-dbe4eb9903be
description:
  "Required permissions: secret.update. Personal keys are capped by the current member
  role; team keys use explicit grants."
url: "{{baseUrl}}/resources/:ownerId/secrets/:environment/:stage"
method: PATCH
headers:
  Accept: application/json
  Content-Type: application/json
pathVariables:
  - key: ownerId
    value: "{{ownerId}}"
    description: Required.
  - key: environment
    value: "{{environment}}"
    description: Required.
  - key: stage
    value: "{{stage}}"
    description: Required.
body:
  type: json
  content: |-
    {
      "expectedRevision": "00000000-0000-4000-8000-000000000000",
      "set": {},
      "delete": []
    }
order: 2000
