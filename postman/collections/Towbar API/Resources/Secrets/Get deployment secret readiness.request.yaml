$kind: http-request
id: b1eb8dcd-1a81-5e29-be6f-b1a7fc6b2b03
description:
  "Required permissions: secret.list. Personal keys are capped by the current member
  role; team keys use explicit grants."
url: "{{baseUrl}}/resources/:ownerId/secrets/readiness"
method: GET
headers:
  Accept: application/json
pathVariables:
  - key: ownerId
    value: "{{ownerId}}"
    description: Required.
order: 2000
