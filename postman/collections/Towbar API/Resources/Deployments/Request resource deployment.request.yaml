$kind: http-request
id: 54a2b43f-dfbc-5b5d-a8a0-1c7ed0619378
description:
  "Required permissions: deployment.create. Personal keys are capped by the current
  member role; team keys use explicit grants."
url: "{{baseUrl}}/resources/:resourceId/actions/deploy"
method: POST
headers:
  Accept: application/json
  Idempotency-Key: string
pathVariables:
  - key: resourceId
    value: "{{resourceId}}"
    description: Required.
order: 5000
