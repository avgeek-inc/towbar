$kind: http-request
id: 41a4f2bf-c719-5181-b503-f2fb656ed2ce
description:
  "Required permissions: deployment.create. Personal keys are capped by the current
  member role; team keys use explicit grants."
url: "{{baseUrl}}/resources/:resourceId/auto-deploy-control"
method: PATCH
headers:
  Accept: application/json
  Content-Type: application/json
pathVariables:
  - key: resourceId
    value: "{{resourceId}}"
    description: Required.
body:
  type: json
  content: |-
    {
      "paused": true
    }
order: 2000
