$kind: http-request
id: 85fbec37-338f-5616-a718-e799545073a1
description:
  "Required permissions: sharedSecret.update. Personal keys are capped by the current
  member role; team keys use explicit grants."
url: "{{baseUrl}}/settings/secrets/:environment/:stage"
method: PATCH
headers:
  Accept: application/json
  Content-Type: application/json
pathVariables:
  - key: environment
    value: "{{environment}}"
    description: Required.
  - key: stage
    value: "{{stage}}"
    description: Required.
body:
  type: json
  content: |-
    {
      "expectedRevision": "00000000-0000-4000-8000-000000000000",
      "set": {},
      "delete": []
    }
order: 2000
