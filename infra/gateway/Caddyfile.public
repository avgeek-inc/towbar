{
	servers {
		trusted_proxies static private_ranges
		trusted_proxies_strict
	}
}

{$TOWBAR_GATEWAY_DOMAIN} {
	tls {
		issuer acme
	}

	@api path /v1/*
	reverse_proxy @api api:4020
	reverse_proxy web-app:4021
}
