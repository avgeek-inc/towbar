# Towbar web client

Browser-safe typed client for the Towbar API. Requests include the host-only API
session cookie and surface the common machine-readable error shape without
ever accepting credential values in response metadata types.
